<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="productSQL">
	<select id="getLargeCategoryList" resultType="category">
		SELECT * FROM category
		WHERE cate_parent IS NULL
		ORDER BY cate_code
	</select>
	
	<select id="getSmallCategoryList" parameterType="String" resultType="category">
		SELECT * FROM category
		WHERE cate_parent=#{cate_parent}
		ORDER BY cate_code
	</select>
	
	<select id="getMyLocation" parameterType="String" resultType="member">
		SELECT mem_location FROM members WHERE mem_id=#{mem_id}
	</select>
	
	<select id="getMyRecentLocation" parameterType="String" resultType="member">
		SELECT mem_recentLocation FROM members WHERE mem_id=#{mem_id}
	</select>
</mapper>
